# Client B: exposes MySQL
# Forward rules are managed dynamically via hubctl
server_addr: "127.0.0.1:7900"
client_name: "client-b"
secret: "my-super-secret-key"
log_level: "info"

expose:
  - name: "mysql"
    local_addr: "127.0.0.1:3306"
    protocol: "tcp"
