# Client A: exposes a local web service
# Forward rules are managed dynamically via hubctl
server_addr: "127.0.0.1:7900"
client_name: "client-a"
secret: "my-super-secret-key"
log_level: "info"

expose:
  - name: "web"
    local_addr: "127.0.0.1:8080"
    protocol: "tcp"
